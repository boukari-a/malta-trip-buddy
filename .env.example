# MongoDB Atlas Connection
# Get this from: MongoDB Atlas → Cluster → Connect → Connect your application
# Replace <username>, <password>, and cluster address with your actual values
MONGO_URL=mongodb+srv://<your-username>:<your-password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Database name (you can keep this as is)
MONGO_DB=malta_trip_buddy

# JWT Configuration
# Change this to a random, secure string for production
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_ALGORITHM=HS256

# Example .env file (REPLACE WITH YOUR ACTUAL VALUES):
# MONGO_URL=mongodb+srv://maltauser:MySecurePass123@cluster0.abc123.mongodb.net/?retryWrites=true&w=majority
# MONGO_DB=malta_trip_buddy
# JWT_SECRET=malta-2024-super-secret-random-key-xyz789
# JWT_ALGORITHM=HS256

# IMPORTANT NOTES:
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit the .env file to Git (it's already in .gitignore)
# 3. Remove < and > brackets from the connection string
# 4. URL encode special characters in your password:
#    ! → %21, @ → %40, # → %23, $ → %24, % → %25, ^ → %5E, & → %26, * → %2A
